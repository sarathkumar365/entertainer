# Claude Code entry point

This file carries no rules of its own. It exists because Claude Code reads `CLAUDE.md` in
preference to `AGENTS.md`, so it points at the file that holds the content.

The repository's operating contract — layout, dependency direction, where new code goes,
style, tests, commit convention, and the hygiene rules that protect a public repo holding
private data:

@AGENTS.md
